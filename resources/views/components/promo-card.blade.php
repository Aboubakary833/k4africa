<div class="promo-single-items">
    <div class="icon">
        <img src="{{ asset($original) }}" alt="{{ $title }}">
        <img src="{{ asset($gradient) }}" alt="{{ $title }}">
        <div class="dot-icon-hover">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </div>
    <div class="content">
        <h4 class="title">{{ $title }}</h4>
        <p>{{ $text }}</p>
    </div>
</div>